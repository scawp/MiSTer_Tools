USE AT OWN RISK!

NOTE: Run on MiSTer via SSH (or PC).

# sortAZ.sh

Sorts files into A-Z sub-directories and disc folders, args - `directory` `extention`

```
sortAZ.sh /media/FAT/Games/PSX/ chd
```
